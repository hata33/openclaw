# shared — 策略

## 一、依赖方向

```
shared ← 其他所有模块
```

shared 不依赖任何业务模块。循环依赖会导致构建失败。

## 二、导出原则

- 纯函数和类型优先
- 有状态的工具需要明确文档说明
- 避免在 shared 中引入运行时副作用

## 三、命名约定

- `normalize*` — 类型规范化
- `resolve*` — 解析/查找
- `as*` — 类型断言
- `is*` — 类型判断
