# agents — 生命周期

## Agent 会话流程

```
1. 创建会话
   → 加载 Agent 配置
   → 初始化上下文

2. 接收消息
   → 路由到对应会话
   → 入队处理

3. 处理消息
   → 组装上下文（历史 + 系统 + 工具）
   → 调用模型
   → 解析响应

4. 工具调用
   → 验证权限（沙箱策略）
   → 执行工具
   → 收集结果

5. 迭代
   → 将工具结果加入上下文
   → 再次调用模型
   → 重复直到无工具调用

6. 返回回复
   → 流式发送到渠道
   → 保存到会话历史
```
