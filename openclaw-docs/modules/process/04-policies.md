# process — 策略

## 一、OOM 保护策略

子进程 `oom_score_adj` 调高，Gateway 进程保持低值。Linux OOM Killer 优先终止子进程。

## 二、终止策略

优雅终止优先（SIGTERM），3 秒后强制终止（SIGKILL）。最大等待 60 秒。

## 三、队列策略

前台命令优先执行，后台命令排队。同一通道的命令串行执行。

## 四、边界情况

### Windows 编码

Windows 子进程输出可能使用 GBK 编码，需要自动检测和转换。

### 大量输出

子进程输出可能很大。使用缓冲限制防止内存溢出。
