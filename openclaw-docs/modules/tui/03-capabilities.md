# tui — API

```typescript
function launchTui(options: TuiLaunchOptions): Promise<void>
function createTuiBackend(params: TuiBackendParams): TuiBackend
function createEmbeddedBackend(params: EmbeddedBackendParams): EmbeddedBackend
```

## 命令

```
/status, /model, /reasoning, /verbose, /clear, /compact, /exit, /help
```
